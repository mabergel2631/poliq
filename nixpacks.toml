[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd apps/api && pip install -r requirements.txt && pip install anthropic openai httpx"]

[start]
cmd = "cd apps/api && uvicorn main:app --host 0.0.0.0 --port 8000"
