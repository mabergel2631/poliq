[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["cd apps/api && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd apps/api && uvicorn main:app --host 0.0.0.0 --port 8000"
