[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd apps/api && pip install -r requirements.txt"]

[start]
cmd = "cd apps/api && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
